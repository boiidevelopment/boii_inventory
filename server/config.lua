config = config or {}

config.inventory_type = 'grid'

config.default_player_inventory = {
    max_slots = 36,
    grid_columns = 10, 
    grid_rows = 10,
    carry_weight = 80000,
    current_weight = 0,
    items = {}
}